main(){
  var list = [1,2,3,4,5,6];
  list.add(7);//Es para poder agregar lo que continua
  print(list);
  //print(list[0]); el lugar de 0 muestra  1
  //print(list.length) // para saber cuantos elementos tiene
  //print(list.last); // para acceder al ultimo elemento
  //print(list.first); // para acceder al primer elemento
}